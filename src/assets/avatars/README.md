# Avatar Assets

Drop avatar image files here. The filename (with extension) becomes the avatar ID.

**Supported formats**: PNG, JPG, JPEG, SVG, GIF, WebP

Example:
- `batman.png` → use `batman.png` as the avatar key in the character picker
- `sonic.gif` → use `sonic.gif` as the avatar key
